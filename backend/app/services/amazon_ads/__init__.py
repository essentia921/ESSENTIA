"""Amazon Ads API adapter (client, report, targets, bids, config).

Consolidato dall'ex pacchetto top-level `amazon_api/`. Amazon Ads e' un adapter,
non il cuore dell'app: qui vivono le chiamate HTTP di basso livello all'API Amazon.
"""
